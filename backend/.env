# Coffee Scheduler Backend - Local Development Environment
# 
# IMPORTANT: This file is only for local development defaults.
# Production environment variables are set in Railway dashboard.

# Server Configuration
PORT=5001
NODE_ENV=development

# Database (local development fallback - Railway uses MONGO_URL from dashboard)
# MONGO_URI=mongodb://localhost:27017/coffee-scheduler

# Google OAuth (set these in Railway dashboard for production)
# GOOGLE_CLIENT_ID=your_google_client_id_here
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# GOOGLE_REDIRECT_URL=https://your-frontend-domain.com

# Session Configuration (Railway uses secure generated secret)
SESSION_SECRET=coffee_scheduler_dev_session_secret_local_only

# CORS Configuration (Railway uses production frontend URL)
FRONTEND_URL=http://localhost:3000
COOKIE_DOMAIN=localhost

# Production variables to set in Railway dashboard:
# - MONGO_URL (from your MongoDB service)
# - GOOGLE_CLIENT_ID 
# - GOOGLE_CLIENT_SECRET
# - GOOGLE_REDIRECT_URL (your production frontend URL)
# - SESSION_SECRET (Railway auto-generates secure secrets)
# - FRONTEND_URL (your production frontend domain)
# - NODE_ENV=production